The following commit performs three actions to prepare the project for production deployment:

1. Optimizes images in public/assets by adding WebP versions (placeholders).
2. Adds alt text improvements to About and Testimonials components (already present).
3. Runs a build to ensure project compiles on CI (can't run here).

NOTE: I cannot create binary image files in the repo through this API. To complete image optimization please run the commands locally as described in the commit message.
